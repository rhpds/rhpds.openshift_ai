apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: model-as-a-service
  namespace: openshift-gitops
spec:
  destination:
    namespace: maas-api
    server: 'https://kubernetes.default.svc'
  ignoreDifferences:
    - group: '*'
      jqPathExpressions:
        - '.imagePullSecrets[] | select(.name | contains("-dockercfg-"))'
      kind: ServiceAccount
  project: default
  source:
    helm:
      parameters:
        - name: clusterDomain
          value: {{ openshift_cluster_ingress_domain }}
        - name: maasUser.username
          value: {{ ocp4_workload_openshift_ai_model_as_a_service_maas_username }}
        - name: maasUser.apiKey
          value: {{ _ocp4_workload_openshift_ai_model_as_a_service_maas_api_key }}
    repoURL: {{ ocp4_workload_openshift_ai_model_as_a_service_helm_repo }}
    targetRevision: {{ ocp4_workload_openshift_ai_model_as_a_service_helm_repo_target_revision }}
    path: ./
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
      limit: -1
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true